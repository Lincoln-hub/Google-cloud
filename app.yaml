runtime: php 
env: flex

runtime_config:
  document_root: 

# Ensure we skip ".env", which is only for local development
skip_files:
  - .env

env_variables:
  # Put production environment variables here.
  APP_LOG: errorlog
  APP_KEY: base64:VudVPUyGasHmwhkaA+EgG7k/86eAu8AMF98ie+RzrPM=